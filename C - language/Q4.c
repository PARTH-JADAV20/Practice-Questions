#include <stdio.h>

int main() {
    int chr='a';
    
    if (chr=='a' || chr=='e' || chr=='i' || chr=='o' || chr=='u'){
        printf("charcter is vowel");
    }else {
        printf("character is consonant");
    }

    return 0;
}